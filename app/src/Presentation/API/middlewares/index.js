const router = require('express').Router();
const jwtMiddleware = require('jwt');

//router.use('/', jwtMiddleware.checkToken, users);
//router.use('/', jwtMiddleware.checkToken, developers);

module.exports = router;
